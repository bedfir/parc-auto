{% extends 'base.html.twig' %}

{% block title %}Voiture index{% endblock %}

{% block body %}
    <h1>Voiture index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Modèle</th>
                <th>Puissance</th>
                <th>Couleur</th>
                <th>NbPortes</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for voiture in voitures %}
            <tr>
                <td>{{ voiture.modele }}</td>
                <td>{{ voiture.puissance }}</td>
                <td>{{ voiture.couleur }}</td>
                <td>{{ voiture.nbPortes }}</td>
                <td>
                    <a href="{{ path('voiture_show', {'slug': voiture.slug}) }}">show</a>
                    <a href="{{ path('voiture_edit', {'slug': voiture.slug}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('voiture_new') }}">Create new</a>
{% endblock %}

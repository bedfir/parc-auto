<form method="post" action="{{ path('voiture_delete', {'slug': voiture.slug}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ voiture.modele) }}">
    <button class="btn">Delete</button>
</form>
